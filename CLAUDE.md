# Workspace Root

This is the default workspace. Each subdirectory is a separate project.

## Structure

```
D:\claude_code\
├── <project-name>/   ← individual project folders
└── ...
```

When working on a project, navigate into its folder. Each project may have its own `CLAUDE.md` with project-specific context.
